# Angular — Synthèse du samedi 18 juillet 2026

## Digest court

Week-end calme côté Angular : aucune release, RFC ou faille en 48 h. **Angular 22**, stable depuis le 3 juin, reste la référence — OnPush par défaut, Signal Forms, Resource API et `httpResource` passés en stable, TypeScript 6 requis. Le compilateur natif Go (**TypeScript 7.0**) est GA, mais l'éditeur Angular reste épinglé sur TS 6.0. Rien de neuf à transformer en mini-cours aujourd'hui : la prochaine itération se prépare, sans annonce publique.

## Top 1 — État de l'écosystème (rien de neuf en 48 h)

- **Angular 22** : toujours la dernière stable ; OnPush + zoneless par défaut, Signal Forms stables — déjà couverts ce mois-ci.
- **TypeScript 7.0** : compilateur Go en GA, mais Angular reste sur TS 6.0 dans l'éditeur — pas d'évolution cette semaine.
- **Rien de neuf** : pas de release, pas de RFC, pas de CVE côté framework — c'est une information en soi.

→ Pas de `_detail.md` aujourd'hui : journée calme, rien de neuf à approfondir en mini-cours.
