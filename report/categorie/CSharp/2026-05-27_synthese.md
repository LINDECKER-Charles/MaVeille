# CSharp / .NET — Synthèse du mercredi 27 mai 2026

## Digest court

Côté Microsoft, la LTS .NET 10 reçoit sa première update de sécurité (10.0.8 du 12 mai) — à déployer rapidement en prod. EF Core 10 introduit le support natif des recherches vectorielles SQL via `VECTOR_DISTANCE()`, ouvrant la voie à du RAG natif dans SQL Server / Azure SQL sans dépendance externe.

## Top des sujets

1. **.NET 10.0.8** — Premier patch de sécurité de la LTS sortie en novembre 2025. Pas de nouvelles features, focus 100 % correctifs. À déployer sous 7 jours sur les workloads exposés.

2. **EF Core 10 vector search** — Support du type SQL `vector` et de `VECTOR_DISTANCE()` (cosine, euclidean, dotproduct). Les migrations EF Core génèrent désormais les index HNSW. Sweet spot : RAG produit jusqu'à quelques millions de vecteurs sans Pinecone/Qdrant.

→ Analyses complètes : `2026-05-27_detail.md`
